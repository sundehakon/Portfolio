import { Typography, Container } from "@mui/material"

const Projects = () => {
    return (
        <Container>
            <Typography>Projects</Typography>
        </Container>
    );
};

export default Projects;