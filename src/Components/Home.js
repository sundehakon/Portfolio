import { Typography, Container } from "@mui/material"

const Home = () => {
    return (
        <Container>
            <Typography>Home</Typography>
        </Container>
    );
};

export default Home;