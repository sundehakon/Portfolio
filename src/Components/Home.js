import { Typography, Container } from "@mui/material"

const Home = () => {
    return (
        <Container>
            <Typography>Håkon Sunde</Typography>
        </Container>
    );
};

export default Home;