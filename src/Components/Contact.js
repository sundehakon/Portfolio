import { Typography, Container } from "@mui/material"

const Contact = () => {
    return (
        <Container>
            <Typography>Contact</Typography>
        </Container>
    );
};

export default Contact;