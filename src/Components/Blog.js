import { Container, Typography } from "@mui/material"

const Blog = () => {
    return (
        <Container>
            <Typography>Coming soon...</Typography>
        </Container>
    );
};

export default Blog;